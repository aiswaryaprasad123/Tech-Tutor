<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Courses</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f8f8;
            color: #333;
        }
        .top-bar {
            background-color: #2600ff;
            color: #fff;
            padding: 1px 0;
            text-align: center;
            font-size: 20px;
            -webkit-text-fill-color: #f8f8f8;
        }

        h1 {
            text-align: center;
            margin-top: 40px;
            color: #333;
        }

        ul {
            list-style-type: none;
            padding: 0;
            margin: 0 auto;
            width: 80%;
        }

        li {
            background-color: #fff;
            border-radius: 8px;
            margin: 20px 0;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(13, 0, 255, 0.899);
        }

        h2 {
            color: #333;
            margin-top: 0; /* Remove default margin from h2 */
        }

        p {
            margin: 10px 0;
        }

        video {
            width: 400px;
            height: auto;
            border-radius: 8px;
            
        }

        .no-courses {
            text-align: center;
            color: #777;
        }
    </style>
</head>

<body>
    <div class="top-bar">
        <h1><i>Tech-Tutor</i></h1>
    </div>
    <h1>My Courses</h1>
    {{#if purchasedCourses}}
    <ul>
        {{#each purchasedCourses}}
        <li>
            <h2 style="color: #2600ff;font-size:40px;">{{this.course.name}}</h2> <!-- Changed to h2 -->
            <p><strong>Duration:</strong> {{this.course.duration}}</p> <!-- Changed to strong tag for emphasis -->
            <p>Amount Paid: {{this.course.pricing}}</p>
            <p>Purchase Date: {{this.purchase.timestamp}}</p>

            <!-- Display contents related to the course -->
            <ul>
                {{#each this.contents}}
                <li>
                    <p>Title: {{this.title}}</p>
                    
                    <video controls>
                        <source src="/videos/{{this._id}}.mp4" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                    <!-- Add more content-related fields here if needed -->
                </li>
                {{/each}}
            </ul>
        </li>
        {{/each}}
    </ul>
    {{else}}
    <p class="no-courses">No courses purchased yet.</p>
    {{/if}}
</body>

</html>
